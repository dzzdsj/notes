

```sql
--行宽
set linesize 2000
--列宽
set pagesize 2000
--不显示标题
set heading off
--字段宽度
col colname format a1000 //todo有问题
--不显示额外信息
set feedback off
//
set flush off
--只显示输出结果到屏幕上，而不会显示SQL语句
set echo off
```

```shell
# sqlplus 环境变量配置
vi $ORACLE_HOME/sqlplus/admin/glogin.sql


```

